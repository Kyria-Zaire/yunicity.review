# Copier vers .env — variables publiques uniquement
EXPO_PUBLIC_API_URL=http://localhost:8000
# Mapbox (carte événements — TICKET-D.5, dev build requis — pas Expo Go)
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=
