# DEV ONLY — variables pour docker compose (substitution locale)
# cp .env.example .env
# Interdiction de réutiliser ces identifiants en recette, preprod ou prod.

POSTGRES_DB=yunicity_dev
POSTGRES_USER=yunicity
POSTGRES_PASSWORD=yunicity_dev_password
POSTGRES_PORT=5434
REDIS_PORT=6379
