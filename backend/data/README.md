# Backend data assets

## `e2e-test-video.mp4`

Minimal MP4 fixture (~17 KiB) for Local Video smoke tests.

Used by `scripts/pilot_m00_seed_videos.py --smoke` (MEDIA-INFRA-V1).

Copied into the API Docker image at `/app/data/e2e-test-video.mp4`.
